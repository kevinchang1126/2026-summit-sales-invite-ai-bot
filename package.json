{
  "name": "2026-summit-sales-invite-ai-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public --compatibility-date=2024-12-01",
    "deploy": "npx wrangler pages deploy --project-name=summit-sales-invite-bot",
    "db:init": "npx wrangler d1 execute DB --remote --file=schema.sql",
    "db:init:local": "npx wrangler d1 execute DB --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
